현재 `MultiLanguageAI_edge-tts-02.py`는 기본 기능이 잘 분리되어 있지만, 실제 사용 안정성과 점수 정확도를 높이려면 다음 항목을 우선 수정하는 것이 좋습니다.

| 우선순위 | 수정 권장 사항 | 문제점 및 개선 방향 |
|---|---|---|
| **높음** | 학습 기록 삭제 인덱스 오류 수정 | 통계 창은 `self.history`를 역순으로 표시하지만, 삭제할 때 화면 인덱스를 원본 목록 인덱스로 그대로 사용합니다. 따라서 사용자가 선택한 기록과 실제 삭제되는 기록이 다를 수 있습니다. 표시 행에 원본 인덱스를 연결하거나, 역순 인덱스로 변환해야 합니다. |
| **높음** | 발음 점수 계산 개선 | `calc_score()`는 목표 단어를 `set`으로 만든 뒤 인식 단어를 세므로 같은 단어를 반복해서 말하면 점수가 100%를 초과할 수 있습니다. 목표 단어와 인식 단어를 순서 또는 빈도까지 고려하고, 최종 점수를 `0~100`으로 제한해야 합니다. |
| **높음** | Tkinter UI 업데이트를 메인 스레드로 통일 | `_listen_thread()`에서 `self.btn_mic.config()`와 `self.lbl_result.config()`를 작업 스레드에서 직접 호출합니다. Tkinter는 일반적으로 메인 스레드에서만 위젯을 변경해야 하므로 간헐적인 멈춤이나 예외가 발생할 수 있습니다. 모든 UI 변경을 `self.after(0, ...)`로 전달해야 합니다. |
| **높음** | TTS 캐시 파일 동시 생성 방지 | 번역 결과 자동 재생과 사용자의 **문장 듣기**가 동시에 실행되면 동일한 `.part` 파일을 사용할 수 있습니다. 파일별 `Lock`을 두거나 임시 파일명을 고유하게 만들어 캐시 손상을 막아야 합니다. |
| **높음** | 번역 엔진을 공식 API로 교체 | 현재 Google GTX와 Chrome 번역 엔드포인트는 공식 공개 API가 아니며 `429` 차단, 응답 형식 변경, 사용 제한에 취약합니다. 운영 안정성이 필요하면 DeepL API 또는 Azure Translator를 주 번역 엔진으로 사용하고, 현재 방식은 보조 경로로 두는 것이 좋습니다. |
| **중간** | 번역 소스 언어 코드 검증 | 자동 감지 결과와 `MultiLanguageAI.json`의 언어 키가 다르면 번역 서비스가 잘못된 언어 코드를 받을 수 있습니다. 설정 파일 로드 시 `languages`의 키, 번역 코드, STT 코드, Edge 음성 매핑을 함께 검증해야 합니다. |
| **중간** | 설정 파일 구조 검증 강화 | 현재는 `languages`와 `ui_texts`가 비어 있는지만 확인합니다. 각 언어에 `name`, `gtts`, `stt`가 있는지, UI 문구에 필요한 키가 모두 있는지 확인하지 않아 실행 중 `KeyError`가 발생할 수 있습니다. |
| **중간** | 패키지 검사 시 GUI 초기화 순서 개선 | `check_packages()`가 Tk 루트 생성 전에 `messagebox.showerror()`를 호출합니다. 환경에 따라 메시지박스가 비정상적으로 표시되거나 종료 처리가 불명확할 수 있습니다. 패키지 검사는 먼저 수행하되, 오류는 로그와 콘솔에 기록하고 GUI 생성 후 표시하거나 명확히 종료하는 구조가 안전합니다. |
| **중간** | 번역 캐시 파일 동시 저장 보호 | 여러 번역 작업이 동시에 끝나면 `translation_cache.json`을 동시에 저장할 수 있습니다. `threading.Lock`을 사용하고 임시 파일에 저장한 뒤 `os.replace()`로 교체하는 방식이 안전합니다. |
| **중간** | 번역 캐시 버전 및 크기 관리 | 언어 코드나 번역 엔진을 변경해도 기존 캐시가 계속 사용됩니다. 캐시 키에 엔진·버전 정보를 포함하거나 오래된 캐시를 삭제할 수 있어야 합니다. 캐시가 무한히 커지는 문제도 제한해야 합니다. |
| **중간** | STT 오류를 사용자 친화적으로 구분 | 현재 예외 메시지를 그대로 표시할 가능성이 있습니다. 마이크 없음, 권한 거부, 음성 타임아웃, 음성 인식 실패, 네트워크 오류를 구분해 안내하면 사용성이 좋아집니다. |
| **중간** | 현재 문장·언어 스냅샷 사용 | 번역이나 TTS 작업이 실행되는 동안 사용자가 언어 또는 문장을 변경하면 작업 스레드가 최신 상태와 섞일 수 있습니다. 작업 시작 시 문장, 소스 언어, 대상 언어, 문장 인덱스를 모두 복사해 작업에 전달해야 합니다. |
| **낮음** | 설정 키 이름 변경 검토 | TTS를 gTTS에서 Edge TTS로 바꾸었지만 JSON의 `"gtts"` 키와 변수명이 남아 있습니다. 현재는 호환성 때문에 동작하지만, 혼동을 줄이려면 `"tts_code"` 또는 `"language_code"`로 변경하고 기존 설정을 한 번 마이그레이션하는 편이 좋습니다. |
| **낮음** | 주석과 실제 동작 일치시키기 | 일부 주석은 여전히 “Google TTS”, “gTTS 보완”, “Google Translate API”라고 설명합니다. 실제 동작은 Edge TTS와 여러 번역 fallback을 사용하므로 유지보수자가 잘못 이해할 수 있습니다. |
| **낮음** | `logging` 중복 설정 방지 | 모듈이 여러 번 import되면 루트 로거에 핸들러가 중복 추가되어 같은 로그가 여러 번 기록될 수 있습니다. 핸들러가 이미 있는지 확인해야 합니다. |
| **낮음** | 종료 처리 추가 | `pygame.mixer.quit()`, 작업 취소, 창 종료 시 리소스 정리 로직이 있으면 더 안정적입니다. 특히 재생 중 창을 닫을 때 발생할 수 있는 예외를 줄일 수 있습니다. |

## 가장 중요한 실제 버그

### 1. 통계 기록 삭제 대상이 어긋날 가능성

현재 통계 목록은 다음처럼 표시됩니다.

```python
for e in reversed(self.history):
    self.tree.insert("", "end", values=(e['date'], f"{e['score']}%"))
```

하지만 삭제할 때는 화면의 인덱스를 그대로 `self.history`에 적용합니다.

```python
indices = [self.tree.index(item) for item in selected]

for idx in sorted(indices, reverse=True):
    del self.history[idx]
```

예를 들어 화면의 첫 번째 기록은 `self.history`의 마지막 기록인데, 현재 코드는 `self.history[0]`을 삭제할 수 있습니다. 이 문제는 가장 먼저 수정하는 것이 좋습니다.

### 2. 점수가 100%를 초과할 수 있음

현재 계산 방식은 다음과 같습니다.

```python
t_set = set(t)
matching_words = sum(1 for w in s if w in t_set)
```

목표 문장이 다음과 같고:

```text
I like apples
```

사용자가 다음처럼 말하면:

```text
I I I like apples apples
```

인식된 단어가 모두 목표 단어 집합에 포함되어 점수가 100%를 넘을 수 있습니다. 최소한 다음과 같이 제한해야 합니다.

```python
return min(100, int((matching_words / len(t)) * 100)) if t else 0
```

더 정확하게 하려면 단어 빈도, 순서, 편집 거리 등을 반영해야 합니다.

### 3. 작업 스레드에서 직접 UI 변경

다음과 같은 코드는 작업 스레드에서 실행될 수 있습니다.

```python
self.btn_mic.config(text="● 지금 말씀하세요", fg=C["danger"])
self.lbl_result.config(text="듣고 있습니다...", fg=C["accent"])
```

다음처럼 메인 스레드로 넘기는 것이 안전합니다.

```python
self.after(
    0,
    lambda: self.btn_mic.config(
        text="● 지금 말씀하세요",
        fg=C["danger"]
    )
)
```

## 추천 수정 순서

1. **통계 기록 삭제 인덱스 오류 수정**
2. **Tkinter UI 업데이트를 메인 스레드로 통일**
3. **발음 점수 계산 및 100점 초과 방지**
4. **TTS 임시 파일·캐시 동시 접근 방지**
5. **번역 요청의 문장·언어 스냅샷 적용**
6. **공식 번역 API 또는 설정 가능한 번역 공급자 구조 도입**
7. **JSON 설정과 주석 정리**
8. **오류 처리 및 종료 처리 강화**

특히 1~4번은 기능 개선이 아니라 실제 오동작이나 간헐적 오류를 예방하는 수정이므로 우선 반영하는 것이 좋습니다.