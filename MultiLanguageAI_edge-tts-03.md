`MultiLanguageAI_edge-tts-03.py`는 Tkinter 기반의 **다국어 발음 학습 프로그램**입니다. 사용자가 외국어 문장을 입력하면 문장별로 나누고, 번역·음성 재생·마이크 발음 인식·점수 기록·통계 분석을 제공합니다.

## 전체 동작 흐름

```text
프로그램 실행
  ↓
필수 모듈 확인
  ↓
MultiLanguageAI.json 로드
  ↓
문장 입력 또는 텍스트 파일 불러오기
  ↓
언어 자동 감지 또는 직접 선택
  ↓
현재 문장 학습
  ├─ 문장 듣기 → Microsoft Edge TTS → MP3 → pygame 재생
  ├─ 문장 번역 → Google GTX → Chrome 번역 → MyMemory
  │                    ↓
  │                 번역 결과 자동 발음
  └─ 발음 연습 → 마이크 → Google STT → 점수 계산 → 기록 저장
```

## 1. 초기화

실행 시 다음 외부 패키지를 확인합니다.

- `edge-tts`: Microsoft Edge 음성 합성
- `pygame`: MP3 재생
- `SpeechRecognition`: 음성 인식
- `requests`: 번역 API 요청
- `langdetect`: 입력 언어 자동 감지

Python 표준 라이브러리인 `tkinter`, `json`, `threading`, `asyncio`, `pathlib` 등은 별도 설치하지 않습니다.

필수 패키지가 없으면 오류 메시지를 표시합니다. 프로그램은 기본적으로 다음 설정 파일을 함께 요구합니다.

```text
MultiLanguageAI.json
```

이 파일은 Python 파일과 같은 폴더에 있어야 합니다.

## 2. 외부 설정 파일 사용

언어와 UI 문구를 코드에 직접 작성하지 않고 `MultiLanguageAI.json`에서 읽습니다.

언어 항목에는 일반적으로 다음 정보가 있습니다.

```json
"en": {
  "name": "영어 - English",
  "gtts": "en",
  "stt": "en-US"
}
```

현재 `"gtts"`라는 키 이름은 기존 설정과의 호환성을 위해 사용하지만, 실제 TTS 엔진은 gTTS가 아닙니다. `EDGE_TTS_VOICE_MAP`에서 해당 언어의 Microsoft Edge 음성을 찾아 사용합니다.

UI 문구는 `ui_texts`에 저장되며, 선택한 학습 언어에 따라 버튼과 라벨이 변경됩니다.

## 3. 백그라운드 작업 처리

프로그램은 `_DaemonThreadPool`이라는 자체 스레드 풀을 사용합니다.

- 데몬 스레드 최대 4개 생성
- 번역, TTS 합성, 음성 인식을 백그라운드에서 실행
- 네트워크 요청 중에도 GUI가 멈추지 않음
- 작업 오류는 `languageai.log`에 기록

Tkinter 위젯은 메인 스레드에서 처리해야 하므로, 음성 인식 작업에서 발생한 UI 변경은 `self.after(0, ...)`를 통해 메인 스레드로 전달합니다.

## 4. 텍스트 처리

사용자가 텍스트를 입력하고 **텍스트 불러오기**를 누르면:

1. 입력 내용의 앞뒤 공백을 제거합니다.
2. 마침표, 물음표, 느낌표, 줄바꿈 등으로 문장을 분리합니다.
3. 첫 번째 문장을 현재 문장으로 설정합니다.
4. 언어를 자동 감지하거나 직접 선택합니다.
5. 현재 문장과 진행 상태를 화면에 표시합니다.

크메르어와 미얀마어에서 사용하는 일부 문장 구분 문자도 지원합니다.

파일 불러오기는 UTF-8 텍스트 파일을 읽어서 같은 처리를 수행합니다.

## 5. 언어 자동 감지

자동 감지를 선택하면 `langdetect.detect()`로 입력 언어를 추정합니다.

```python
code = detect(text).lower()
```

감지된 코드가 `MultiLanguageAI.json`에 있으면 해당 언어를 사용합니다. 설정에 없는 언어이거나 감지에 실패하면 영어로 처리합니다.

주의할 점은 짧은 문장이나 여러 언어가 섞인 문장에서는 자동 감지가 부정확할 수 있다는 것입니다. 이런 경우 언어 콤보박스에서 직접 선택하는 것이 더 정확합니다.

## 6. 번역 기능

현재 문장에 대해 **문장 번역** 버튼을 누르면 백그라운드 스레드에서 번역합니다.

번역 순서는 다음과 같습니다.

1. 디스크에 저장된 번역 캐시 확인
2. Google GTX 엔드포인트 요청
3. 실패 또는 `429` 발생 시 Google Chrome 번역 엔드포인트 요청
4. Chrome 번역도 실패하면 MyMemory API 요청
5. 결과를 `translation_cache.json`에 저장
6. 화면에 번역 결과 표시
7. 번역된 문장을 Edge TTS로 자동 재생

번역 캐시는 다음 형태의 키를 사용합니다.

```text
출발언어:목표언어:원문
```

예:

```text
en:ko:Hello world
```

중국어 코드처럼 서비스별 표기가 다른 경우 일부 코드를 정규화합니다.

```text
zh-cn → zh-CN
zh-tw → zh-TW
```

번역 작업을 시작할 때 문장·출발 언어·목표 언어를 복사해 전달하므로, 번역 중 사용자가 다른 문장이나 언어를 선택해도 진행 중인 요청의 입력이 바뀌지 않습니다.

단, Google GTX와 Chrome 번역 엔드포인트는 공식 번역 API가 아니므로 `429` 차단이나 응답 형식 변경 가능성이 있습니다.

## 7. Microsoft Edge TTS

### 음성 매핑

`EDGE_TTS_VOICE_MAP`이 언어 코드와 Edge 음성을 연결합니다.

예:

```python
"ko": "ko-KR-HyunsuMultilingualNeural"
"en": "en-US-JennyNeural"
"ja": "ja-JP-NanamiNeural"
"zh-cn": "zh-CN-XiaoxiaoNeural"
```

### 음성 생성 과정

1. 현재 문장과 언어 코드 확인
2. Edge 음성 매핑 조회
3. MP3 캐시 확인
4. 캐시가 없으면 Edge TTS로 합성
5. `.part` 임시 파일에 저장
6. 파일이 정상적으로 생성되었는지 확인
7. `os.replace()`로 최종 MP3 파일 생성
8. `pygame`으로 재생

임시 파일을 사용하는 이유는 네트워크 오류로 불완전한 MP3가 캐시되는 것을 방지하기 위해서입니다.

### 재시도와 동시성

- Edge TTS 합성은 최대 3회 재시도
- MP3 재생 실패 시 캐시 삭제 후 최대 2회 재합성
- 같은 캐시 파일의 동시 생성은 파일별 잠금으로 보호
- `pygame` 오디오 재생 자체도 `_audio_lock`으로 한 번에 하나만 처리

따라서 번역 결과 자동 발음과 사용자의 문장 듣기가 동시에 실행될 때 발생할 수 있는 파일 충돌과 오디오 충돌을 줄였습니다.

## 8. 우즈베크어 전처리

코드에는 우즈베크어 키릴 문자를 라틴 문자로 바꾸는 함수가 포함되어 있습니다.

```python
_uzbek_cyrillic_to_latin()
```

우즈베크어 문장에 키릴 문자가 포함되어 있으면 Edge TTS 전송 전에 내부적으로 변환합니다. 화면에 표시하는 원문은 변경하지 않습니다.

다만 `MultiLanguageAI.json`에 우즈베크어를 추가하고, Python 파일의 `EDGE_TTS_VOICE_MAP`에도 다음과 같은 항목을 추가해야 실제로 사용할 수 있습니다.

```python
"uz": "uz-UZ-SardorNeural"
```

## 9. 발음 점수 계산

마이크 음성은 `SpeechRecognition`을 통해 Google 음성 인식 서비스로 텍스트화됩니다.

점수 계산은 다음 방식입니다.

1. 목표 문장과 인식 결과를 소문자로 변환
2. 문장부호 제거
3. 단어 단위로 분리
4. 목표 단어의 출현 횟수를 기록
5. 인식 결과와 목표 단어를 순서대로 비교
6. 실제로 일치한 단어 수를 목표 단어 수로 나누어 백분율 계산

수정 전에는 목표 단어를 단순한 `set`으로 비교했기 때문에 같은 단어를 반복하면 점수가 부풀 수 있었습니다. 현재 버전은 단어 빈도를 반영합니다.

예를 들어 목표가:

```text
I like apples
```

이고 사용자가:

```text
I I I like apples apples
```

라고 말해도 점수는 100점을 넘지 않습니다.

## 10. 학습 기록

발음 연습이 끝나면 다음 데이터를 저장합니다.

```json
{
  "date": "2026-09-15",
  "score": 85
}
```

새 기록은 `history.jsonl` 파일에 한 줄씩 추가합니다.

- 새 기록 추가: 파일 끝에 한 줄 추가
- 기록 삭제: 전체 기록 다시 저장
- 기존 `history.json`만 있는 경우: `history.jsonl`로 마이그레이션

사용자별 문장이나 음성 내용 자체는 학습 기록에 저장하지 않고 날짜와 점수만 저장합니다.

## 11. 학습 통계 창

**학습 성취도 분석**을 누르면 별도 통계 창이 열립니다.

제공 내용:

- 오늘 평균 점수
- 최근 7일 평균 점수
- 최근 30일 평균 점수
- 최근 7일 막대그래프
- 날짜별 점수 목록
- 선택한 기록 삭제
- 목록 새로 고침

기록 목록은 최신 항목부터 표시되지만, 각 Treeview 행에 원본 기록 인덱스를 부여합니다. 따라서 화면 순서와 내부 기록 순서가 달라도 선택한 항목을 정확히 삭제할 수 있습니다.

## 12. 파일과 캐시

프로그램은 사용자 데이터 폴더에 다음 자료를 저장합니다.

```text
%APPDATA%\MultiLangAI\
├─ history.json
├─ history.jsonl
├─ translation_cache.json
└─ cache\
   └─ *.mp3
```

MP3 캐시는 30일이 지난 파일을 자동으로 삭제합니다.

## 13. 현재 버전에서 개선된 부분

`03` 버전에는 다음 문제가 개선되어 있습니다.

- 통계 기록 삭제 대상 불일치
- 발음 점수 100점 초과 가능성
- 작업 스레드의 직접적인 Tkinter UI 변경
- 번역 중 문장·언어 변경으로 인한 데이터 혼선
- TTS 캐시 파일 동시 생성 충돌
- 동시에 여러 음성을 재생할 때의 `pygame` 충돌
- 번역 캐시 동시 저장 문제

## 14. 현재 남아 있는 주의점

- 번역은 공식 Google API가 아니라 비공식 Google 엔드포인트에 의존합니다.
- 실제 음성 합성과 번역에는 인터넷 연결이 필요합니다.
- 발음 인식에는 마이크와 `PyAudio`가 필요할 수 있습니다.
- `MultiLanguageAI.json`의 언어 항목과 `EDGE_TTS_VOICE_MAP`이 일치해야 합니다.
- Edge TTS는 Microsoft Edge 음성 서비스에 연결되므로, 네트워크·방화벽·서비스 정책의 영향을 받을 수 있습니다.
- `gtts`라는 JSON 키 이름은 남아 있어 TTS 엔진이 Google인 것처럼 보일 수 있습니다. 향후에는 `tts_code` 또는 `language_code`로 변경하는 것이 유지보수에 더 적합합니다.

핵심적으로 이 프로그램은 **Tkinter GUI + 백그라운드 작업 스레드 + Google 계열 번역 fallback + Microsoft Edge TTS + Google STT + JSONL 학습 기록**으로 구성된 다국어 발음 학습 애플리케이션입니다.